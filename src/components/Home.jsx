import React, { useState } from "react";
import "./Home.scss";

const Home = () => {
  function handleClick() {}
  const [isClicked, setIsClicked] = useState(false);

  return (
    <div className="yoga-series">
      <div className="neck-yoga">dkdkdk</div>
    </div>
  );
};

export default Home;
